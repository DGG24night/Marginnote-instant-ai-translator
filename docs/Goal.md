# Marginnote 即时 AI 翻译插件

一款运行在 Marginnote 中的划词翻译插件。目标用户是在 Marginnote 中阅读英文文献/学术文章的用户：选中文字后即时获得 AI 翻译或词典释义，不打断阅读流。

## 主要功能

### 一、AI 翻译

1. 用户选中文本后自动触发翻译（可在设置中切换为"点击悬浮按钮后触发"）。
2. 翻译结果显示在选中文本附近的悬浮卡片中，不遮挡原文；靠近窗口边缘时自动调整位置。
3. 翻译结果支持流式输出；请求失败或超时需有明确错误提示。
4. 支持一键复制翻译结果。

### 二、查词

1. 选中内容为单个英文单词时，自动调用有道词典查词并自动发音（可在设置中关闭发音，支持英/美音选择）。
2. 用户可在查词结果卡片上一键切换为"AI 解释"。
3. 支持一键复制查词结果。
4. 判定规则：单个英文单词走查词逻辑，其余（词组、句子、段落）走翻译逻辑。

### 三、插件面板与设置

1. 设置完成后可关闭面板；划词时结果以悬浮卡片自动出现。
2. 支持配置多个 AI 服务提供商，每个提供商下可保存多个模型。
3. 可分别为查词和翻译指定提供商与模型，并可设置 temperature 和 reasoning effort。
4. 支持自定义查词/翻译 prompt，提供默认模板，支持 `{text}`、`{target_lang}` 等变量占位符。
5. 结果文字清晰可见，字号可在设置中调节。
6. UI 简洁，支持亮/暗两种主题。

## 其他要求

- 翻译方向默认英→中，目标语言可在设置中切换。
- API key 仅存储在本地，不向任何第三方发送。
- 非目标：不做全文翻译、不做 OCR。